#ifndef GLOBAL_H
#define GLOBAL_H

namespace Global {
	const float WIDTH = 480, HEIGHT = 270, WIN_SCALE = 3, WIN_WIDTH = WIDTH * WIN_SCALE, WIN_HEIGHT = HEIGHT * WIN_SCALE;
	const float VIRTUAL_FRAMERATE = 60;
	const bool START_FULLSCREEN = false;
};

#endif