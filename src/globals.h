#ifndef GLOBAL_H
#define GLOBAL_H

namespace Global {
	const int WIDTH = 640, HEIGHT = 360, WIN_SCALE = 2, WIN_WIDTH = WIDTH * WIN_SCALE, WIN_HEIGHT = HEIGHT * WIN_SCALE;
	const float VIRTUAL_FRAMERATE = 60;
};

#endif